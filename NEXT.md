# NEXT — open decisions and follow-ups

_Last updated 2026-05-18. This is a working doc. Delete items as they land; add items as new ones surface._

## 1. Merge or close two upstream PRs

These were opened during the audit cleanup and are mergeable but left for you to review:

| PR | Repo | What it does | If you merge | If you decline |
|---|---|---|---|---|
| [auth-b2c#1](https://github.com/convergent-systems-co/auth-b2c/pull/1) | `auth-b2c` | Trim brew `description` to 71 chars and split `system` args in `.goreleaser.yaml` | Next release regenerates `Formula/auth-b2c.rb` correctly. The hand-fix from tap PR #4 becomes a no-op. | The hand-fix in this tap will regress every time auth-b2c cuts a release. |
| [ai#5](https://github.com/convergent-systems-co/ai/pull/5) | `ai` | Move `def install` to top level in `.github/templates/homebrew.rb.tmpl` | Next ai release regenerates `Formula/ai.rb` correctly. The hand-fix from tap PR #10 becomes a no-op. | Hand-fix regresses on every ai release (this just happened with v0.5.0 → tap PR #10). |

**Decision needed:** merge or close, per PR. There is no third option that doesn't regress.

## 2. Olympus generator — decide whether to fix or not

You explicitly said "leave olympus alone" during the audit cleanup. That decision still stands, but it has a cost. Capture the cost here so future-you knows the trade.

- `Formula/olympus.rb` is generated by `convergent-systems-co/olympus/.github/workflows/release-and-publish.yml`. The license input is at line 248 (`License: BSL-1.1`) and a formula template lives somewhere under that repo I didn't fully locate.
- **What will regress on the next olympus release:**
  - `license "BSL-1.1"` (not a valid SPDX → `brew audit --strict` fails on SPDX check).
  - `desc` likely back to 85 chars (`FormulaAudit/Desc` fails).
  - `define_method(:install)` inside `on_macos` / `on_linux` (`FormulaAudit/ComponentsOrder` fails).
- When that happens, the tap's `Test Formula` workflow goes red on `brew audit --formula` and `brew style`. Hand-patch (same as tap PR #4) buys time but regresses again on the next release after that.

**Decision needed:** when the next olympus release lands and CI goes red, do you (a) fix the generator at that point, (b) keep hand-patching, or (c) leave the formula broken in the tap because olympus distribution is happening elsewhere?

## 3. Watchpoints — automatic regressions to expect

Things that **will** break without intervention:

```
ai release        → regenerates Formula/ai.rb       → fixed iff ai#5 merged
auth-b2c release  → regenerates Formula/auth-b2c.rb → fixed iff auth-b2c#1 merged
olympus release   → regenerates Formula/olympus.rb  → broken until §2 is addressed
gitignore release → regenerates Formula/gitignore.rb → unchanged by the release;
                    only the auto-update workflow rewrites it
```

If `Test Formula` goes red after a release, check which formula was just bumped against this list before opening a fresh investigation.

## 4. Gitignore — Linux ARM gap

Upstream `convergent-systems-co/gitignore` builds `darwin-amd64`, `darwin-arm64`, `linux-amd64`, `windows-amd64`. No `linux-arm64`.

Current formula has `on_linux do; on_intel do; … end; end` only. Today's audit accepts this. If a future `brew audit` change tightens that — flagging "no URL on linux-arm64" — you have two paths:

| Option | What to do | Trade-off |
|---|---|---|
| Add `linux-arm64` upstream | Extend the matrix in `convergent-systems-co/gitignore/.github/workflows/release.yml` to include `goos: linux, goarch: arm64`, then add an `on_arm do` block in `Formula/gitignore.rb` and a fourth artifact in `update-gitignore.yml`. | One-time work. Full Linux coverage. |
| Scope the formula explicitly | Add `disable! on: :linux_arm64, because: "no build available"` or similar. | Brittle; depends on Homebrew supporting per-platform `disable!` (verify before relying on it). |

**Decision needed:** only if/when audit starts flagging this. Not urgent today.

## 5. Node 24 cutover — confirm and clean up

- `.github/workflows/test-formula.yml` carries `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"` at the job level. CI annotations confirm `actions/cache@v4` is now being forced to Node 24.
- **2026-06-02:** GitHub forces this for everyone. After that date, the env var is harmless but redundant.
- **When `actions/cache` ships a v5 with native Node-24 support:** bump the `uses:` pin to `actions/cache@v5` and remove the env var.

**Decision needed:** none today. Calendar item for after 2026-06-02 to review.

## 6. Update-gitignore workflow — verify post-revert

PR #9 changed `update-gitignore.yml` from writing `Casks/gitignore.rb` back to writing `Formula/gitignore.rb`, added a linux-amd64 download/sha step, and added a third regex stanza. **It has not been run yet against a real release.**

**Action:** at the next gitignore release (or via `workflow_dispatch` with an explicit version), confirm:

- [ ] `current=$(grep version Formula/gitignore.rb)` resolves correctly.
- [ ] All three artifacts download.
- [ ] All three sha256 stanzas update (not just darwin).
- [ ] Commit message reads "chore: update gitignore to vX.Y.Z" (not "cask").

If any step fails, the regex in lines 93-110 is the most likely culprit.

## 7. Things explicitly out of scope (don't put them back in)

- **Casks for gitignore** — was the wrong call; reverted in PR #9. Don't reintroduce unless upstream stops shipping Linux/Windows.
- **Windows install via brew** — Homebrew has no Windows story. Windows users get separate "download + extract zip" instructions from the gitignore README.
- **archive/olympus.rb** — deleted in PR #5. Recoverable from git log if needed but should stay deleted.
